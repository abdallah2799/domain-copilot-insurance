# Business Requirements Document — Domain Copilot (Insurance Claims Adjudication)

**Status**: living document, updated as each requirement is implemented. See the traceability matrix at the bottom for current state.

## 1. Context

An insurance organization's adjusters spend significant time cross-referencing policy documents, coverage schedules, and exclusion clauses to adjudicate claims, and risk two specific failure modes: applying an outdated policy version, and trusting an LLM's arithmetic for payout/limit/deductible calculations. Domain Copilot ingests the policy and claims corpus, answers coverage questions with verifiable citations, and runs a three-agent adjudication workflow (coverage matching → exclusion analysis → drafting) that always routes through a human adjuster before any decision is finalized.

## 2. Assigned variant

- **Domain**: D2 — Insurance claims adjudication
- **Twist**: T6 — Document in/out (OCR ingestion with confidence handling; generated DOCX/PDF adjudication memo with citations and tables)
- **Derivation**: stated directly in the assignment invitation email, not derived from National ID.

## 3. Personas

| Persona | Role | Needs |
|---|---|---|
| **Adjuster** | Mandated by the D2 approval gate; the only role that may approve, reject, or edit-and-approve an adjudication decision. | Fast, grounded coverage answers with citations; full visibility into what each agent did and why before signing off; cannot be forced to accept an ungrounded or version-ambiguous recommendation. |
| **Analyst** | FR-8's second required role (ADR-0012): can ingest, ask, and start adjudication runs, but never finalize one. | Can start and monitor their own runs end-to-end; cannot approve/reject/edit-and-approve any run, including their own, and cannot see another Analyst's runs (object-ownership check). |

## 4. Objectives (measurable)

| ID | Objective | Measurable acceptance criterion |
|---|---|---|
| OBJ-1 | Every claim answer is grounded or explicitly refused | 0% of golden-set answers make an unsupported claim; refusal rate on out-of-corpus questions matches expected refusals in `docs/EVALUATION.md` |
| OBJ-2 | No payout/limit/deductible figure is ever LLM-computed | 100% of such figures trace to `PayoutCalculationService` unit-tested output, verifiable by code review and contract tests |
| OBJ-3 | No decision reaches the adjuster without having passed through all three agents | Enforced by orchestration state machine; verified by run-trace inspection (any run ID) |

## 5. Requirements (BR-xx) — grows per epic

Numbering matches the epics in the engineering plan; acceptance criteria are filled in as each is implemented, and the traceability matrix (§8) tracks status.

| ID | Requirement | Acceptance criteria (summary) |
|---|---|---|
| BR-01 | Public repo with governance (branch protection, PR/issue templates, CODEOWNERS, CI) exists before feature work begins | Repo created, protected `main`, templates present |
| BR-02 | Clean Architecture solution skeleton with enforced inward dependency direction | `Domain`/`Application` reference no LLM/vector/web-framework SDK; verified by project reference graph |
| BR-03 | Provider abstraction: `ICompletionService`/`IEmbeddingService` ports with OpenAI + Ollama implementations and a documented fallback chain | Swapping provider is config + one adapter (ADR-0003); fallback chain unit-tested with fakes, no network dependency |
| BR-04 | Synthetic corpus of ≥30 documents / 150+ pages, no real personal data, with ≥2 dated policy versions carrying documented substantive differences (D2's named version-risk) and at least one document category requiring OCR (T6) | 145 documents generated reproducibly from `seed-data/generate/`; the searchable knowledge-corpus subset (policy-forms/endorsements/reference — the categories actually embedded and indexed per ADR-0004) is 60 documents / 151 pages, clearing the ≥30-doc/150-page floor on its own; scanned claim forms verified to have no extractable text layer (`pdftotext` → ~0 bytes) |
| BR-05 | Relational store (MSSQL via EF Core, with migrations) and vector store (Qdrant) both genuinely wired, not just referenced — includes the first real domain entity (`Document`, driven by FR-1's ingestion-tracking requirement) and readiness health checks proving actual connectivity | Migration applied against a live containerized MSSQL; `/health/ready` returns Healthy only when both MSSQL and Qdrant respond; 4 Testcontainers-based integration tests exercise the repository against a real (ephemeral) SQL Server, including a SQL-Server-enforced unique-constraint case |
| BR-06 | FR-1 ingestion pipeline for the knowledge corpus (extract → clean → chunk → embed → index), triggered via a documented HTTP endpoint (doubling as the brief's required "seed/ingest command"), idempotent, with per-document status reporting | `POST /api/ingestion/knowledge-corpus` run against the real corpus: 60/60 documents completed, 0 failures, 552 chunks, cross-checked against Qdrant's own point count and `GET /api/documents`; re-run confirmed idempotent (unchanged `chunkCount`/`points_count`/document timestamps, response time dropped from ~85s to <60ms); 20 new unit tests (chunker + cleaner) |
| BR-07 | FR-2 retrieval: hybrid dense+keyword search with a documented fusion method (the brief's required "justified enhancement" beyond plain semantic search), version/date-aware metadata filtering directly mitigating D2's named wrong-policy-version risk, structured citations traceable to an exact chunk, and correct refusal on low-evidence queries | `GET /api/retrieval/search` run against the live corpus (ADR-0005): a relevant query scored 0.74–0.81 dense cosine similarity and was accepted; an off-corpus query scored ≤0.39 and was correctly refused; explicit version filtering verified to exclude the non-matching policy edition at `topK=20`, not just rank it lower; 18 new unit tests (BM25 scoring, RRF fusion, version resolution) |
| BR-08 | D2's non-negotiable control: every payout/limit/deductible figure is computed by plain, unit-tested C# — never an LLM — and exposed to agents only as a gated tool call (`CLAUDE.md`, "Architecture boundaries") | 4 Domain calculators (`DomainCopilot.Domain.Adjudication`, zero external deps) each implementing one cited corpus formula (Claims Adjudication Guidelines Step 4; Total Loss Valuation Methodology Sections 1/3/5); 4 matching `IToolExecutor` tools with JSON Schema contracts and explicit required-argument validation (a missing argument fails rather than silently defaulting to 0); 56 new unit/contract tests (ADR-0006) |
| BR-09 | Structured case-data foundation for the planned agents' lookup tools (`lookup_declarations`, `lookup_claim_history`) — Declarations/claim-history facts persisted relationally, sourced from the corpus generator's own structured data rather than re-parsed from generated prose (ADR-0007) | `POST /api/casedata/load` run against the live corpus: 18 policy declarations + 18 claim history records loaded, cross-checked against a direct SQL query (loss-type distribution matching the corpus exactly); re-run confirmed idempotent (0 loaded, 36 skipped); 29 new unit/integration tests, including 8 Testcontainers-based tests proving the Endorsements JSON-column conversion and date-window query work against real SQL Server |
| BR-10 | FR-5's approval gate as a structural control, not a courtesy step: an adjudication run's own aggregate enforces that every pipeline stage runs in order and that no recommendation reaches a terminal (approved/rejected/edited-and-approved) state without passing through `AwaitingApproval` first, plus the 5th tool FR-4 requires — the one write/side-effecting tool, gated behind approval (ADR-0008) | `AdjudicationCase` state machine (`DomainCopilot.Domain.Adjudication`) with guard clauses on every transition; `finalize_adjudication_decision` wraps Approve/Reject/EditAndApprove; 29 new unit/integration/contract tests (out-of-order transitions throwing, reject requiring a non-empty reason, a claim number verified not unique-constrained — a claim can be reopened as a new run); full DI composition root verified to resolve by starting the real API and confirming `/health/ready` |
| BR-11 | FR-4/FR-5's multi-agent workflow: 4 named agents (Coverage Matcher, Anomaly Analyst, Exclusion Analyst, Adjudication Drafter) sharing one restricted-tool-set/typed-I/O/termination-condition tool-calling loop, driven by a fixed pipeline orchestrator through `AdjudicationCase`'s stages; prompts as versioned files with 2-3 worked examples each (ADR-0009) | 6 real end-to-end runs against the live corpus on local Ollama (OpenRouter deliberately blanked to conserve hosted quota per the user's direction): Coverage Matcher completed correctly twice in a row (real, correct, persisted `CoverageMatchResult`); 4 genuine bugs found and fixed (timeout too short for real tool-calling latency, a tool call emitted as text, a correct answer buried in prose, misleading error messages); Anomaly Analyst's non-convergence on this local model documented honestly as a known limitation, not silently worked around — see ADR-0009's Verification section in full; 17 new unit tests for the tool-calling loop itself |
| BR-12 | T6's OCR ingestion: scanned claim documents processed with real per-page confidence, routing below-threshold documents to `NeedsReview` rather than silently trusting a bad extraction (ADR-0010) | `POST /api/ocr/documents` verified live against both a clean and a genuinely degraded real scan; one real bug found and fixed via that live testing (Tesseract reporting high confidence on a page with zero recognized text) |
| BR-13 | T6's generated document-out: a PDF adjudication memo with embedded citations and a coverage/limits/exclusions table, rendered from the same stage data the chat answer and approval gate already use (ADR-0011) | `GET /api/adjudication/runs/{id}/memo` verified via real PdfPig text-extraction round-trip tests; a real culture-dependent formatting bug (`decimal.ToString("C")`) found via an actual CI failure and fixed |
| BR-14 | FR-3's evaluation harness: a ≥25-entry golden Q/A set (≥5 adversarial, ≥3 prompt-injection, a conflicting-policy-version pair) and a real, runnable black-box harness reporting hit-rate/groundedness/refusal correctness | `docs/evaluation/golden-set.json` (32 entries); `tools/DomainCopilot.EvaluationHarness`; `docs/EVALUATION.md` reports a real 7-entry live sample honestly, including two real failures (an ambiguous-question refusal gap, and a genuine version-confusion answer reproducing D2's named central risk) |
| BR-15 | FR-8's authentication and object-level authorization: JWT auth, two genuinely different server-enforced roles, and an object-ownership check on adjudication runs (ADR-0012) | Live-verified: no token → 401, wrong role on approve → 403, an Analyst's `ListRuns` scoped to their own cases only, an Adjuster seeing all cases; a real pre-existing gap fixed as a result (the approval audit trail's actor field, previously an unverified client-supplied string, now always the authenticated caller's username) |
| BR-16 | FR-9's observability: a correlation ID flowing request→orchestrator→agent→LLM call, OpenTelemetry traces to a self-hosted viewer, and persisted per-request token/cost accounting (ADR-0013) | Live-verified: a real `X-Correlation-Id` response header, the self-hosted Aspire Dashboard container starting correctly (a real OTLP-port mismatch caught and fixed along the way), `GET /api/observability/token-usage` authenticating and returning a well-formed report; the record/query round-trip itself verified via a real Testcontainers MSSQL test, since one live end-to-end attempt hit the same Ollama-latency constraint BR-14 also discloses |
| *(BR-17+)* | Remaining FR-9 hardening beyond the floor, `docs/SYSTEM-DESIGN.md`/`docs/ARCHITECTURE.md`/`docs/SECURITY.md` (now present, living documents updated per epic going forward), teaching pack, deployment | To be added/updated as each is built — see `docs/SYSTEM-DESIGN.md`'s gap table for what remains and why |

## 6. Out of scope (explicit)

- Real personal data of any kind (hard non-negotiable per assessment brief) — corpus is synthetic/public only.
- A third or later insurance product line beyond a single representative policy family used to build the corpus.
- Anything beyond T6's core (OCR confidence handling + one generated document type) — broader document-type support is deferred, see `docs/SYSTEM-DESIGN.md` gap table.
- Semantic/vector search over case data (declarations, claims) — deliberately out of scope; see ADR-0004. Case data is fetched by exact key (policy/claim number), never searched, and is not embedded into Qdrant.

## 7. Assumptions & risks

- **Assumption**: "invitation email" variant statement (D2T6) is authoritative and does not need National-ID derivation math shown.
- **Assumption**: "the document corpus" that FR-2's "ask with citations" retrieves over means the knowledge corpus (policy wordings, exclusions, endorsement templates, reference material) specifically, not case data (declarations/claims) — see ADR-0004.
- **Risk (named by the brief)**: wrong-policy-version retrieval — mitigated by the version/date metadata schema (ADR-0004) and, concretely, by `PolicyVersionResolver`'s deterministic date-to-version lookup plus mandatory filtering in hybrid retrieval (ADR-0005), verified against real queries.
- **Risk (named by the brief)**: arithmetic hallucination — mitigated structurally by four Domain-layer deterministic calculators exposed to agents only as gated tool calls, never as something the model computes itself (ADR-0006).
- **Risk (schedule)**: only 6 calendar days remain versus the brief's 12-day assumption; MoSCoW floors (3 agents+orchestrator, 30-doc corpus, one retrieval enhancement, T6 core only) are treated as the actual target. Any further cut will be logged in the `SYSTEM-DESIGN.md` gap table, not silently dropped.

## 8. Traceability matrix

| BR-xx | Implemented? | Evidence |
|---|---|---|
| BR-01 | Implemented | `.github/`, `LICENSE`, `CONTRIBUTING.md`, `.env.example`, branch protection on `main`, CI green |
| BR-02 | Implemented | `DomainCopilot.slnx`, `src/*` project reference graph (Domain has no external deps; Application depends only on Domain) |
| BR-03 | Implemented | `src/DomainCopilot.Application/Providers/`, `src/DomainCopilot.Infrastructure/Providers/`, `docs/adr/0003-provider-abstraction-fallback-chain.md`, `tests/DomainCopilot.Application.Tests/Providers/FallbackCompletionServiceTests.cs`, `tests/DomainCopilot.Contract.Tests/KernelToolMapperTests.cs` |
| BR-04 | Implemented | `seed-data/corpus/` (145 docs total; 60 docs / 151 pages in the searchable knowledge-corpus subset), `seed-data/generate/facts.py`, `seed-data/README.md` |
| BR-05 | Implemented | `src/DomainCopilot.Domain/Documents/`, `src/DomainCopilot.Infrastructure/Persistence/`, `src/DomainCopilot.Infrastructure/VectorStore/`, `tests/DomainCopilot.Integration.Tests/DocumentRepositoryTests.cs`, `tests/DomainCopilot.Domain.Tests/Documents/DocumentTests.cs` |
| BR-06 | Implemented | `src/DomainCopilot.Application/Ingestion/`, `src/DomainCopilot.Infrastructure/Ingestion/`, `src/DomainCopilot.Api/Controllers/IngestionController.cs`, `docs/adr/0004-chunking-and-knowledge-vs-case-data-split.md`, `tests/DomainCopilot.Application.Tests/Ingestion/` |
| BR-07 | Implemented | `src/DomainCopilot.Application/Retrieval/`, `src/DomainCopilot.Infrastructure/Retrieval/`, `src/DomainCopilot.Api/Controllers/RetrievalController.cs`, `docs/adr/0005-hybrid-retrieval-and-version-aware-filtering.md`, `tests/DomainCopilot.Application.Tests/Retrieval/` |
| BR-08 | Implemented | `src/DomainCopilot.Domain/Adjudication/`, `src/DomainCopilot.Application/Adjudication/`, `docs/adr/0006-deterministic-payout-calculation.md`, `tests/DomainCopilot.Domain.Tests/Adjudication/`, `tests/DomainCopilot.Application.Tests/Adjudication/`, `tests/DomainCopilot.Contract.Tests/ToolDefinitionsMapToKernelFunctionsTests.cs` |
| BR-09 | Implemented | `src/DomainCopilot.Domain/CaseData/`, `src/DomainCopilot.Application/CaseData/`, `src/DomainCopilot.Infrastructure/Persistence/CaseData/`, `src/DomainCopilot.Api/Controllers/CaseDataController.cs`, `docs/adr/0007-case-data-foundation-for-agent-tools.md`, `tests/DomainCopilot.Domain.Tests/CaseData/`, `tests/DomainCopilot.Application.Tests/CaseData/`, `tests/DomainCopilot.Integration.Tests/PolicyDeclarationRepositoryTests.cs`, `tests/DomainCopilot.Integration.Tests/ClaimHistoryRepositoryTests.cs` |
| BR-10 | Implemented | `src/DomainCopilot.Domain/Adjudication/AdjudicationCase.cs`, `src/DomainCopilot.Domain/Adjudication/AdjudicationRunStatus.cs`, `src/DomainCopilot.Application/Adjudication/FinalizeAdjudicationDecisionToolExecutor.cs`, `src/DomainCopilot.Infrastructure/Persistence/Adjudication/`, `docs/adr/0008-approval-gate-state-machine.md`, `tests/DomainCopilot.Domain.Tests/Adjudication/AdjudicationCaseTests.cs`, `tests/DomainCopilot.Integration.Tests/AdjudicationCaseRepositoryTests.cs` |
| BR-11 | Implemented (Anomaly Analyst's local-model convergence is a documented open item, not silently claimed — see ADR-0009) | `src/DomainCopilot.Application/Adjudication/AgentRunner.cs`, `CoverageMatcherAgent.cs`, `AnomalyAnalystAgent.cs`, `ExclusionAnalystAgent.cs`, `AdjudicationDrafterAgent.cs`, `AdjudicationOrchestrator.cs`, `prompts/*.md`, `src/DomainCopilot.Api/Controllers/AdjudicationController.cs`, `docs/adr/0009-multi-agent-orchestrator.md`, `tests/DomainCopilot.Application.Tests/Adjudication/AgentRunnerTests.cs` |
| BR-12 | Implemented | `src/DomainCopilot.Domain/Ocr/`, `src/DomainCopilot.Application/Ocr/`, `src/DomainCopilot.Infrastructure/Ocr/`, `src/DomainCopilot.Api/Controllers/OcrController.cs`, `docs/adr/0010-ocr-confidence-and-review-routing.md`, `tests/DomainCopilot.Integration.Tests/OcrPipelineTests.cs` |
| BR-13 | Implemented | `src/DomainCopilot.Application/Adjudication/AdjudicationMemoService.cs`, `src/DomainCopilot.Infrastructure/Adjudication/AdjudicationMemoGenerator.cs`, `docs/adr/0011-generated-adjudication-memo.md`, `tests/DomainCopilot.Contract.Tests/AdjudicationMemoGeneratorTests.cs` |
| BR-14 | Implemented (7-entry live sample, not the full 32-entry set — see `docs/SYSTEM-DESIGN.md` gap table) | `docs/evaluation/golden-set.json`, `tools/DomainCopilot.EvaluationHarness/`, `docs/EVALUATION.md` |
| BR-15 | Implemented | `src/DomainCopilot.Domain/Identity/`, `src/DomainCopilot.Application/Identity/`, `src/DomainCopilot.Infrastructure/Identity/`, `src/DomainCopilot.Api/Controllers/AuthController.cs`, `docs/adr/0012-authentication-and-object-level-authorization.md`, `tests/DomainCopilot.Domain.Tests/Identity/`, `tests/DomainCopilot.Application.Tests/Identity/`, `tests/DomainCopilot.Integration.Tests/UserRepositoryTests.cs` |
| BR-16 | Implemented | `src/DomainCopilot.Application/Observability/`, `src/DomainCopilot.Infrastructure/Observability/`, `src/DomainCopilot.Api/Controllers/ObservabilityController.cs`, `docs/adr/0013-observability-correlation-tracing-and-cost-accounting.md`, `tests/DomainCopilot.Integration.Tests/TokenUsageObservabilityTests.cs` |
